# Railway Management - คู่มือการใช้งาน

## ภาพรวม

ระบบ Railway Management ช่วยให้คุณควบคุมและตั้งเวลาเปิดปิด Railway services ผ่าน Web UI เพื่อลดค่าใช้จ่าย

### คุณสมบัติ

✅ **ควบคุมด้วย Web UI** - เปิด/ปิด services ผ่าน path `/railway`
✅ **ตั้งเวลาอัตโนมัติ** - กำหนดเวลาเปิดปิดตามวันและเวลาที่ต้องการ
✅ **รองรับ Vercel** - Deploy บน Vercel ได้ทันทีโดยไม่ต้องติดตั้ง cron บนเครื่อง
✅ **บันทึก Logs** - ติดตามประวัติการเปิดปิด services
✅ **ปลอดภัย** - ใช้ Supabase RLS และ API Token

---

## 🚀 วิธีการติดตั้งและใช้งาน

### ขั้นตอนที่ 1: สร้าง Railway API Token

1. ไปที่ https://railway.com/account/tokens
2. คลิก "Create New Token"
3. ตั้งชื่อ: "Scheduler Token"
4. คัดลอก token ไว้ใช้ในขั้นตอนถัดไป

### ขั้นตอนที่ 2: เปิดใช้งานหน้า Railway Management

1. เปิดแอปพลิเคชัน
2. ไปที่ `/railway` (เช่น https://yourdomain.vercel.app/railway)
3. คลิก "ตั้งค่า API Token"
4. วาง Railway API Token ที่คัดลอกไว้
5. คลิก "บันทึก"

### ขั้นตอนที่ 3: ควบคุม Services แบบ Manual

#### แท็บ "ควบคุม Services":

1. **เลือก Project** - เลือก Railway project ที่ต้องการจัดการ
2. **ดู Service Status** - ดูสถานะว่า service กำลัง running หรือ stopped
3. **เปิด/ปิด Service** - คลิกปุ่ม "เปิด Service" หรือ "ปิด Service"
4. **รีเฟรชสถานะ** - คลิก "รีเฟรช" เพื่ออัพเดทสถานะล่าสุด

### ขั้นตอนที่ 4: ตั้งเวลาอัตโนมัติ

#### แท็บ "ตั้งเวลาอัตโนมัติ":

1. **เลือก Service** - เลือก service ที่ต้องการตั้งเวลา
2. **เลือก Environment** - เลือก environment (production, staging, etc.)
3. **กำหนดเวลาเปิด** - ระบุเวลาที่ต้องการเปิด service (เช่น 08:00)
4. **กำหนดเวลาปิด** - ระบุเวลาที่ต้องการปิด service (เช่น 18:00)
5. **เลือกวันทำงาน** - เลือกวันที่ต้องการให้ทำงาน (จ.-ศ., ทุกวัน, etc.)
6. คลิก **"สร้างตารางเวลา"**

#### ตัวอย่างการตั้งค่า:

```
Service: web-api
Environment: production
เวลาเปิด: 08:00
เวลาปิด: 18:00
วันทำงาน: จ. อ. พ. พฤ. ศ. (จันทร์-ศุกร์)
```

**ผลลัพธ์:** Service จะเปิดอัตโนมัติ 08:00 น. และปิดอัตโนมัติ 18:00 น. ทุกวันจันทร์-ศุกร์

### ขั้นตอนที่ 5: ตั้งค่า Supabase Cron (สำคัญมาก!)

เพื่อให้ตารางเวลาทำงานอัตโนมัติ คุณต้องตั้งค่า Cron job ใน Supabase:

#### วิธีที่ 1: ผ่าน Supabase Dashboard (แนะนำ)

1. ไปที่ https://supabase.com/dashboard/project/YOUR_PROJECT_ID/database/cron-jobs
2. คลิก "Create a new cron job"
3. ตั้งค่าดังนี้:
   ```
   Name: Railway Scheduler
   Schedule: */5 * * * * (ทุก 5 นาที)
   Function URL: https://YOUR_PROJECT_ID.supabase.co/functions/v1/railway-scheduler
   HTTP Method: POST
   ```
4. คลิก "Create cron job"

#### วิธีที่ 2: ผ่าน SQL (สำหรับผู้เชี่ยวชาญ)

```sql
SELECT cron.schedule(
  'railway-scheduler',
  '*/5 * * * *',
  $$
  SELECT net.http_post(
    url:='https://YOUR_PROJECT_ID.supabase.co/functions/v1/railway-scheduler',
    headers:='{"Content-Type": "application/json"}'::jsonb,
    body:='{}'::jsonb
  ) as request_id;
  $$
);
```

**หมายเหตุ:** ตั้งให้รันทุก 5 นาทีเพื่อให้แม่นยำ (สามารถเปลี่ยนเป็นทุก 1 นาทีได้)

---

## 💰 การประหยัดค่าใช้จ่าย

### ตัวอย่างการคำนวณ

**สถานการณ์:** Development environment ที่ไม่ได้ใช้งานนอกเวลาทำงาน

#### ก่อนใช้ Railway Scheduler:
- เปิดตลอด 24/7 = **730 ชั่วโมง/เดือน**
- ค่าใช้จ่าย: **$50/เดือน** (สมมติ)

#### หลังใช้ Railway Scheduler (จ.-ศ. 08:00-18:00):
- เปิด: 10 ชม./วัน × 22 วันทำงาน = **220 ชั่วโมง/เดือน**
- ปิด: **510 ชั่วโมง/เดือน**
- ค่าใช้จ่าย: **$15/เดือน**
- **ประหยัด: $35/เดือน (70%)**

### กรณีใช้งานที่แนะนำ

✅ Development/Staging environments
✅ Worker services ที่ทำงานเฉพาะช่วงเวลา
✅ Background jobs ที่ไม่ต้องทำงานตลอด 24/7
✅ Database instances สำหรับ testing

❌ **ไม่แนะนำ** สำหรับ Production services ที่ต้องพร้อมใช้งานตลอดเวลา

---

## 🔧 การจัดการตารางเวลา

### ดูตารางเวลาที่สร้างไว้

ในแท็บ "ตั้งเวลาอัตโนมัติ" จะแสดงรายการตารางเวลาทั้งหมด:

- **ชื่อ Service** - service ที่ตั้งเวลาไว้
- **เวลาเปิด-ปิด** - เวลาที่กำหนด
- **วันทำงาน** - วันที่จะทำงาน
- **สถานะ** - เปิด/ปิดการทำงาน (Switch)
- **ลบ** - ลบตารางเวลา

### เปิด/ปิดการทำงานของตารางเวลา

- คลิก **Switch** เพื่อเปิด/ปิดการทำงานชั่วคราว
- ไม่ต้องลบตารางเวลาออก สามารถปิดแล้วเปิดใหม่ได้ทีหลัง

### ดู Logs การเปิดปิด

ดู logs ผ่าน Supabase Dashboard:

1. ไปที่ https://supabase.com/dashboard/project/YOUR_PROJECT_ID/editor
2. เลือกตาราง `railway_logs`
3. ดูประวัติการเปิดปิด services

---

## 🌐 Deploy บน Vercel

### ทำไมใช้งานบน Vercel ได้?

ระบบนี้ใช้ **Supabase Edge Functions** และ **Supabase Cron** แทน cron บนเครื่อง:

1. **Frontend** (React) - รันบน Vercel
2. **Scheduler Logic** - รันบน Supabase Edge Function
3. **Cron Trigger** - รันบน Supabase Cron (cloud-based)

ดังนั้น **ไม่ต้องมี server หรือ cron daemon** บนเครื่องของคุณเอง!

### ขั้นตอน Deploy

```bash
# 1. Build project
npm run build

# 2. Deploy to Vercel
vercel --prod

# 3. ตั้งค่า Environment Variables ใน Vercel Dashboard
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
```

---

## 📊 ตัวอย่างการใช้งานจริง

### กรณีที่ 1: Development Environment

**ปัญหา:** Dev environment เปิดตลอดทำให้เสียค่าใช้จ่าย
**วิธีแก้:**

```
Service: dev-api, dev-worker, dev-redis
เวลาเปิด: 08:00
เวลาปิด: 18:00
วันทำงาน: จันทร์-ศุกร์
```

**ผลลัพธ์:** ประหยัด ~70% ค่าใช้จ่าย

### กรณีที่ 2: Nightly Worker

**ปัญหา:** Worker ต้องการรันเฉพาะกลางคืน
**วิธีแก้:**

```
Service: nightly-worker
เวลาเปิด: 22:00
เวลาปิด: 06:00
วันทำงาน: ทุกวัน
```

### กรณีที่ 3: Weekend Testing

**ปัญหา:** Staging environment ไม่ได้ใช้ช่วงสุดสัปดาห์
**วิธีแก้:**

สร้าง 2 ตารางเวลา:

**ตารางที่ 1 (จันทร์-ศุกร์):**
```
เวลาเปิด: 08:00
เวลาปิด: 18:00
วันทำงาน: จ.-ศ.
```

**ตารางที่ 2 (ปิดทั้งวันเสาร์-อาทิตย์):**
```
เวลาปิด: 00:01
วันทำงาน: ส.-อา.
```

---

## ⚠️ ข้อควรระวัง

### 1. Timezone

- **Supabase Cron ใช้ UTC timezone**
- ตรวจสอบว่าเวลาที่ตั้งคือเวลาท้องถิ่นของคุณหรือ UTC
- สำหรับประเทศไทย (UTC+7): ตั้งเวลาลบ 7 ชั่วโมง
  - ต้องการเปิด 08:00 ICT → ตั้งเป็น 01:00 UTC
  - ต้องการปิด 18:00 ICT → ตั้งเป็น 11:00 UTC

### 2. Service Dependencies

- ถ้า service A ต้องการ service B ให้ทำงานก่อน
- ตั้งเวลาเปิด service B ให้เร็วกว่า service A สักครู่

### 3. Database Services

- **ระวัง!** การปิด database อาจทำให้ข้อมูลสูญหาย
- ใช้เฉพาะกับ test databases เท่านั้น

### 4. API Rate Limits

- Railway API มี rate limit
- ถ้ามี services เยอะ อาจต้องเพิ่มช่วง delay

---

## 🔍 Troubleshooting

### ตารางเวลาไม่ทำงาน

1. **ตรวจสอบ Supabase Cron**
   - เข้า Supabase Dashboard → Database → Cron Jobs
   - ดูว่า cron job ถูกสร้างและเปิดอยู่หรือไม่

2. **ตรวจสอบ Edge Function Logs**
   ```bash
   # ดู logs จาก Supabase Dashboard
   # Functions → railway-scheduler → Logs
   ```

3. **ตรวจสอบ railway_logs table**
   ```sql
   SELECT * FROM railway_logs
   ORDER BY created_at DESC
   LIMIT 10;
   ```

### Railway API Error

1. **ตรวจสอบ API Token**
   - Token อาจหมดอายุ → สร้างใหม่
   - ไปที่ `/railway` → คลิก "ตั้งค่า API Token"

2. **ตรวจสอบ Service ID & Environment ID**
   - ID อาจเปลี่ยนหลังจาก redeploy
   - ลบตารางเวลาเก่า สร้างใหม่

### Cron ไม่รันเวลาที่ถูกต้อง

- ตรวจสอบ **Timezone**
- Supabase Cron ใช้ UTC
- ปรับเวลาให้ตรงกับ timezone ของคุณ

---

## 📚 เอกสารเพิ่มเติม

- [Railway API Documentation](https://docs.railway.com/reference/public-api)
- [Supabase Cron Jobs](https://supabase.com/docs/guides/database/extensions/pg_cron)
- [Supabase Edge Functions](https://supabase.com/docs/guides/functions)

---

## 🆘 ต้องการความช่วยเหลือ?

หากพบปัญหาหรือมีคำถาม:

1. ตรวจสอบ **Logs** ใน Supabase Dashboard
2. ตรวจสอบ **railway_logs** table
3. ตรวจสอบ **Cron job status** ใน Supabase
4. ทดสอบการเปิดปิด manual ก่อนตั้งเวลา

---

**สนุกกับการประหยัดค่าใช้จ่าย Railway!** 💰
