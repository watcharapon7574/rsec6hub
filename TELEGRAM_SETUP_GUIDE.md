# คู่มือตั้งค่าระบบแจ้งเตือน Telegram - ฉบับย่อ

## 📋 สิ่งที่ต้องเตรียม

1. ~~**Telegram Bot Token** - จาก @BotFather~~ ✅ **ฝังไว้ใน code แล้ว**
2. **Chat ID** ของแต่ละคน - จาก @userinfobot
3. **Supabase Access Token** - จาก Supabase Dashboard

---

## 🚀 ขั้นตอนการติดตั้ง (4 ขั้นตอน)

### ~~ขั้นตอนที่ 1: สร้าง Telegram Bot~~ ✅ เสร็จแล้ว

Bot Token ถูกฝังไว้ใน code แล้ว: `7677125075:AAGH-NAyoaHdtkmizGuVM3EQeGrwBfnz2fQ`

### ขั้นตอนที่ 1: หา Chat ID ของแต่ละคน (30 วินาที/คน)

**วิธีที่ง่ายที่สุด:**
1. แต่ละคนเปิด Telegram หา **@userinfobot**
2. ส่งข้อความอะไรก็ได้
3. Bot จะตอบกลับพร้อม **Chat ID** (ตัวเลข 9 หลัก เช่น 123456789)
4. บันทึก Chat ID ไว้

### ขั้นตอนที่ 2: Deploy Edge Function (3 นาที)

```bash
# 1. ตั้งค่า Access Token (ดูขั้นตอนด้านล่าง)
export SUPABASE_ACCESS_TOKEN="your_access_token"

# 2. Login
supabase login

# 3. Link project (ดู Project Ref ที่ Dashboard > Settings > General)
supabase link --project-ref your-project-ref

# 4. Deploy function (Bot Token ฝังไว้ใน code แล้ว ไม่ต้องตั้งค่า secrets)
supabase functions deploy telegram-notify
```

**วิธีหา Supabase Access Token:**
1. ไปที่ https://supabase.com/dashboard
2. คลิกโปรไฟล์ (มุมบนขวา) > **Account Settings**
3. เลือก **Access Tokens**
4. คลิก **Generate New Token**
5. ตั้งชื่อ "CLI Access" และคัดลอก Token

### ขั้นตอนที่ 3: Run Database Migration (2 นาที)

**⚠️ สำคัญ**: ใช้ไฟล์ `20250114000002_telegram_notifications_hardcoded.sql` (มี hardcoded values)

**Option 1: ใช้ SQL Editor ใน Supabase Dashboard** (แนะนำ)
1. ไปที่ [Supabase SQL Editor](https://supabase.com/dashboard/project/ikfioqvjrhquiyeylmsv/sql/new)
2. **Migration 1**: Copy-paste จาก `supabase/migrations/20250114000000_add_doc_del_column.sql` → กด **Run**
3. **Migration 2**: Copy-paste จาก `supabase/migrations/20250114000002_telegram_notifications_hardcoded.sql` → กด **Run**

**Option 2: ใช้ CLI** (อาจมีปัญหา migration history)
```bash
supabase db push
```

### ขั้นตอนที่ 4: ตั้งค่า Database Config (2 นาที)

ไปที่ Supabase Dashboard > **SQL Editor** และรันคำสั่งนี้:

```sql
-- แทนที่ your-project-ref ด้วย Project Ref จริง
-- หา Project Ref ได้ที่ Settings > General
ALTER DATABASE postgres SET app.supabase_url TO 'https://your-project-ref.supabase.co';

-- หา Anon Key ได้ที่ Settings > API > Project API keys > anon public
ALTER DATABASE postgres SET app.supabase_anon_key TO 'your_anon_key_here';
```

---

## 👥 ขั้นตอนสำหรับผู้ใช้แต่ละคน (1 นาที/คน)

### 1. Start Bot
ให้แต่ละคนเปิด Telegram และ:
1. ค้นหา Bot ที่คุณสร้าง (username ที่ตั้งไว้)
2. กด **Start** หรือส่งข้อความอะไรก็ได้

### 2. บันทึก Chat ID ลง Database

**Option 1: ใช้ SQL Editor**
```sql
UPDATE employees
SET telegram_chat_id = '123456789'  -- Chat ID ที่ได้จาก @userinfobot
WHERE user_id = 'user-uuid-here';
```

**Option 2: สร้างหน้า Settings ใน Frontend**
ให้ผู้ใช้กรอก Chat ID เองผ่านหน้า Settings (แนะนำ)

---

## ✅ ทดสอบว่าใช้งานได้

### ทดสอบส่งข้อความ Manual (ตรวจสอบ Chat ID)

```bash
# แทนที่ 123456789 ด้วย Chat ID จริงของคุณ
curl https://api.telegram.org/bot7677125075:AAGH-NAyoaHdtkmizGuVM3EQeGrwBfnz2fQ/sendMessage \
  -d "chat_id=123456789" \
  -d "text=ทดสอบระบบแจ้งเตือน ✅"
```

ถ้าได้รับข้อความใน Telegram แสดงว่า **Chat ID** ถูกต้อง ✅

### ทดสอบ Trigger (ตรวจสอบว่าระบบทำงานอัตโนมัติ)

```sql
-- สร้างเอกสารทดสอบ แล้วเปลี่ยนสถานะเป็น rejected
UPDATE memos
SET status = 'rejected',
    reject_reason = 'ทดสอบระบบแจ้งเตือน'
WHERE id = 'memo-id-ที่มีอยู่แล้ว';
```

ผู้เขียนเอกสารควรได้รับการแจ้งเตือนใน Telegram ภายใน 1-5 วินาที ✅

---

## 📊 ระบบแจ้งเตือนที่ได้รับ

### 1. เอกสารถูกตีกลับ (Rejected)
- **ผู้รับ**: ผู้เขียนเอกสาร
- **เงื่อนไข**: `status` เปลี่ยนเป็น `rejected`

### 2. ถึงคิวลงนาม (Pending Sign)
- **ผู้รับ**: ผู้ที่ต้องลงนามตามลำดับ (`current_signer_order`)
- **เงื่อนไข**: `status` เปลี่ยนเป็น `pending_sign`

### 3. เอกสารเสร็จสิ้น (Completed)
- **ผู้รับ**: ผู้เขียนเอกสาร
- **เงื่อนไข**: `status` เปลี่ยนเป็น `completed`

---

## 🔧 Troubleshooting เบื้องต้น

### ไม่ได้รับการแจ้งเตือน?

1. **ตรวจสอบว่า user มี telegram_chat_id**:
```sql
SELECT first_name, last_name, telegram_chat_id
FROM employees
WHERE user_id = 'user-id';
```

2. **ตรวจสอบว่า user Start Bot แล้ว**:
   - ต้องกด Start ใน Bot ก่อน Telegram จึงจะอนุญาตให้ส่งข้อความได้

3. **ดู Logs**:
   - Database Logs: ดูว่า trigger ทำงานไหม
   - Edge Function Logs: ดูว่า HTTP request สำเร็จไหม

4. **ทดสอบ Bot Token**:
```bash
curl https://api.telegram.org/botYOUR_BOT_TOKEN/getMe
```

---

## 📚 เอกสารเพิ่มเติม

ดูคู่มือฉบับเต็มที่: `supabase/functions/telegram-notify/README.md`

---

## 🎯 สรุป Checklist

- [x] ~~สร้าง Telegram Bot และได้ Bot Token~~ ✅ ฝังไว้ใน code แล้ว
- [ ] ให้แต่ละคนหา Chat ID จาก @userinfobot
- [ ] Deploy Edge Function ด้วย `supabase functions deploy telegram-notify`
- [x] ~~ตั้งค่า `TELEGRAM_BOT_TOKEN` secret~~ ✅ ไม่ต้องทำ (ฝังไว้ใน code แล้ว)
- [ ] Run migration `20250114000001_telegram_notifications.sql`
- [ ] ตั้งค่า `app.supabase_url` และ `app.supabase_anon_key`
- [ ] บันทึก `telegram_chat_id` ของแต่ละคนใน database
- [ ] ให้แต่ละคน Start Bot (ค้นหา `@rsec6hub_bot` ใน Telegram)
- [ ] ทดสอบส่งข้อความ manual
- [ ] ทดสอบ trigger โดยเปลี่ยน status เอกสาร

เสร็จแล้ว! ระบบแจ้งเตือนพร้อมใช้งาน 🎉
