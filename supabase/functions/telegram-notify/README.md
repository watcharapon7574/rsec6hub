# Telegram Notification Edge Function

ฟังก์ชันนี้ใช้สำหรับส่งการแจ้งเตือนไปยัง Telegram เมื่อมีเหตุการณ์เกี่ยวกับเอกสาร

## การตั้งค่า Telegram Bot

### 1. สร้าง Telegram Bot

1. เปิด Telegram และค้นหา **@BotFather**
2. ส่งคำสั่ง `/newbot`
3. ตั้งชื่อ Bot เช่น "Doc Management Notifications"
4. ตั้ง username เช่น "doc_management_bot"
5. คัดลอก **Bot Token** ที่ได้รับ (จะมีรูปแบบคล้าย `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 2. หา Chat ID สำหรับผู้ใช้แต่ละคน

**วิธีที่ 1** - ใช้ @userinfobot (ง่ายที่สุด):
1. แต่ละคนค้นหา **@userinfobot** ใน Telegram
2. ส่งข้อความอะไรก็ได้ให้มัน
3. มันจะตอบกลับมาพร้อม Chat ID (ตัวเลขยาวๆ เช่น 123456789)
4. บันทึก Chat ID นี้

**วิธีที่ 2** - ส่งข้อความหา Bot แล้วดู Chat ID:
```bash
# 1. ให้ผู้ใช้ส่งข้อความอะไรก็ได้หา Bot ที่คุณสร้าง (ต้องส่งก่อนจึงจะมี chat)
# 2. แทนที่ YOUR_BOT_TOKEN ด้วย Token ที่ได้จาก @BotFather
curl https://api.telegram.org/botYOUR_BOT_TOKEN/getUpdates

# ตัวอย่าง Response:
# {
#   "ok": true,
#   "result": [{
#     "message": {
#       "chat": {
#         "id": 123456789,  <-- นี่คือ Chat ID ของผู้ใช้
#         "first_name": "Your Name",
#         "type": "private"
#       }
#     }
#   }]
# }
```

### 3. บันทึก Chat ID ลง Database

แต่ละผู้ใช้ต้องบันทึก `telegram_chat_id` ของตัวเองใน table `employees`:

```sql
-- อัปเดต telegram_chat_id สำหรับผู้ใช้
UPDATE employees
SET telegram_chat_id = '123456789'  -- Chat ID ที่ได้จากขั้นตอนที่ 2
WHERE user_id = 'user-uuid-here';
```

**หมายเหตุ**: ระบบจะส่งการแจ้งเตือนเฉพาะผู้ใช้ที่มี `telegram_chat_id` เท่านั้น

## การ Deploy

### ขั้นตอนที่ 1: ตั้งค่า Environment Variables

เข้าไปที่ Supabase Dashboard:
1. เลือก Project ของคุณ
2. ไปที่ **Settings** > **Edge Functions**
3. เพิ่ม Environment Variables:
   - `TELEGRAM_BOT_TOKEN`: Token ที่ได้จาก @BotFather

**หมายเหตุ**: ไม่ต้องตั้ง `TELEGRAM_CHAT_ID` แบบ global เพราะระบบจะใช้ `telegram_chat_id` จาก database แต่ละคนแทน

### ขั้นตอนที่ 2: Deploy ผ่าน Supabase CLI

```bash
# 1. Login to Supabase (ถ้ายังไม่ได้ login)
export SUPABASE_ACCESS_TOKEN="your_supabase_token"
supabase login

# 2. Link project (ถ้ายังไม่ได้ link)
supabase link --project-ref YOUR_PROJECT_REF

# 3. Deploy function
supabase functions deploy telegram-notify

# 4. Set bot token secret (ทำครั้งเดียว)
supabase secrets set TELEGRAM_BOT_TOKEN="your_bot_token_here"
```

### ขั้นตอนที่ 3: Run Database Migration

Migration นี้จะสร้าง triggers อัตโนมัติสำหรับส่งการแจ้งเตือน:

```bash
# เข้าไปที่ Supabase SQL Editor และรัน migration file:
# supabase/migrations/20250114000001_telegram_notifications.sql
```

หรือใช้ Supabase CLI:

```bash
supabase db push
```

### ขั้นตอนที่ 4: ตั้งค่า Database Settings

ต้องตั้งค่า `app.supabase_url` และ `app.supabase_anon_key` ใน Supabase Dashboard:

1. ไปที่ **Database** > **Settings** > **Custom Postgres Config**
2. เพิ่ม settings:
```sql
-- Set in Supabase SQL Editor
ALTER DATABASE postgres SET app.supabase_url TO 'https://your-project-ref.supabase.co';
ALTER DATABASE postgres SET app.supabase_anon_key TO 'your_anon_key_here';
```

**หรือ** ใช้ environment variables ใน migration แทน

### ขั้นตอนที่ 5: ทดสอบระบบ

1. **ทดสอบ Edge Function โดยตรง**:
```bash
curl -X POST https://your-project-ref.supabase.co/functions/v1/telegram-notify \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_ANON_KEY" \
  -d '{
    "type": "document_pending",
    "document_id": "test-123",
    "document_type": "memo",
    "subject": "ทดสอบการแจ้งเตือน",
    "author_name": "ผู้ทดสอบ",
    "current_signer_name": "ผู้อนุมัติ",
    "chat_id": "123456789"
  }'
```

2. **ทดสอบ Database Trigger**:
```sql
-- สร้างเอกสารทดสอบ และเปลี่ยนสถานะ
UPDATE memos
SET status = 'rejected',
    reject_reason = 'ทดสอบการแจ้งเตือน'
WHERE id = 'your-test-memo-id';

-- ตรวจสอบ logs ที่ Database Logs หรือ Edge Function Logs
```

## การทำงานของระบบ (Automatic Notifications)

ระบบจะส่งการแจ้งเตือนอัตโนมัติผ่าน **Database Triggers** ใน 3 กรณี:

### 1. เอกสารถูกตีกลับ (status = 'rejected')
- **ผู้รับแจ้งเตือน**: ผู้เขียนเอกสาร (author)
- **ข้อความ**: แจ้งว่าเอกสารถูกปฏิเสธพร้อมเหตุผล
- **Trigger**: เมื่อ `status` เปลี่ยนเป็น `rejected`

### 2. ถึงคิวลงนาม (status = 'pending_sign')
- **ผู้รับแจ้งเตือน**: ผู้ที่ต้องลงนามตาม `current_signer_order`
- **ข้อความ**: แจ้งว่ามีเอกสารรอการพิจารณา
- **Trigger**: เมื่อ `status` เปลี่ยนเป็น `pending_sign`

### 3. เอกสารเสร็จสิ้น (status = 'completed')
- **ผู้รับแจ้งเตือน**: ผู้เขียนเอกสาร (author)
- **ข้อความ**: แจ้งว่าเอกสารผ่านการอนุมัติเรียบร้อย
- **Trigger**: เมื่อ `status` เปลี่ยนเป็น `completed`

**หมายเหตุ**:
- ระบบจะส่งการแจ้งเตือนเฉพาะผู้ใช้ที่มี `telegram_chat_id` ใน table `employees` เท่านั้น
- ถ้าไม่มี `telegram_chat_id` จะข้ามการส่งและบันทึก log ไว้
- ไม่ส่งการแจ้งเตือนสำหรับเอกสารที่ถูก soft delete (`doc_del IS NOT NULL`)

## ประเภทการแจ้งเตือนที่รองรับ

1. **document_pending**: เอกสารรอการพิจารณา (ส่งถึงผู้ที่ต้องลงนาม)
2. **document_approved**: เอกสารได้รับการอนุมัติ (ส่งถึงผู้เขียน)
3. **document_rejected**: เอกสารถูกปฏิเสธ (ส่งถึงผู้เขียน)

## ตัวอย่างข้อความแจ้งเตือน

### กรณีตีกลับ (Rejected):
```
❌ แจ้งเตือนเอกสาร

เอกสารถูกปฏิเสธ
เรื่อง: ขออนุมัติจัดซื้อวัสดุ
ผู้สร้าง: นายสมชาย ใจดี
เหตุผล: ข้อมูลไม่ครบถ้วน กรุณาเพิ่มรายละเอียดงบประมาณ

กรุณาตรวจสอบและแก้ไขเอกสาร

🔗 ID: abc-123-def
```

### กรณีรอลงนาม (Pending):
```
📝 แจ้งเตือนเอกสาร

เอกสารใหม่รอการพิจารณา
เรื่อง: ขออนุมัติจัดซื้อวัสดุ
ผู้สร้าง: นายสมชาย ใจดี
รอการพิจารณาจาก: นายสมศักดิ์ รักษา
ตำแหน่ง: รองผู้อำนวยการ

กรุณาเข้าระบบเพื่อพิจารณาเอกสาร

🔗 ID: abc-123-def
```

### กรณีเสร็จสิ้น (Completed):
```
✅ แจ้งเตือนเอกสาร

เอกสารได้รับการอนุมัติแล้ว
เรื่อง: ขออนุมัติจัดซื้อวัสดุ
ผู้สร้าง: นายสมชาย ใจดี
เลขที่หนังสือ: ศธ 123/2568

เอกสารผ่านการอนุมัติเรียบร้อยแล้ว

🔗 ID: abc-123-def
```

## ข้อควรระวัง

1. **จำกัดการแจ้งเตือน**:
   - Supabase Free Tier: 500,000 Edge Function invocations/month
   - คำนวณ: ถ้ามี 100 เอกสาร/วัน × 3 การแจ้งเตือน/เอกสาร × 30 วัน = 9,000 invocations/month (เพียงพอ)

2. **ความปลอดภัย**:
   - `TELEGRAM_BOT_TOKEN` เก็บเป็น Secret ใน Supabase
   - `telegram_chat_id` เก็บใน database (ไม่ sensitive มาก)
   - ไม่ควร expose Bot Token ใน frontend code

3. **Rate Limiting**:
   - Telegram API: 30 messages/second per bot
   - ระบบของเราส่งแบบ async ผ่าน pg_net ไม่น่าจะเกินลิมิต

4. **Error Handling**:
   - Trigger function จะ log error แต่ไม่ fail transaction หลัก
   - ถ้าส่ง Telegram ไม่สำเร็จ เอกสารยังถูก update ตามปกติ
   - ตรวจสอบ errors ได้ที่ Database Logs และ Edge Function Logs

## Troubleshooting

### ไม่ได้รับการแจ้งเตือน

1. **ตรวจสอบ `telegram_chat_id` ใน database**:
```sql
SELECT user_id, first_name, last_name, telegram_chat_id
FROM employees
WHERE user_id = 'your-user-id';
```

2. **ตรวจสอบว่าผู้ใช้ส่งข้อความหา Bot แล้ว**:
   - ผู้ใช้ต้องเริ่มการสนทนากับ Bot ก่อน (Start หรือส่งข้อความใดๆ)
   - Telegram จะไม่อนุญาตให้ Bot ส่งข้อความไปหาผู้ใช้ที่ยังไม่เคยติดต่อมา

3. **ตรวจสอบ Bot Token**:
```bash
# Test bot token
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getMe
```

4. **ดู logs**:
   - Database Logs: ดู trigger logs (`RAISE LOG` messages)
   - Edge Function Logs: ดู HTTP request/response logs

5. **ทดสอบส่ง manual**:
```bash
curl https://api.telegram.org/bot<YOUR_BOT_TOKEN>/sendMessage \
  -d "chat_id=123456789" \
  -d "text=Test message"
```

### Trigger ไม่ทำงาน

1. **ตรวจสอบว่า trigger ถูกสร้างแล้ว**:
```sql
SELECT trigger_name, event_manipulation, event_object_table
FROM information_schema.triggers
WHERE trigger_name LIKE '%telegram%';
```

2. **ตรวจสอบว่า status เปลี่ยนจริง**:
```sql
-- ดู status ของเอกสาร
SELECT id, subject, status, created_at, updated_at
FROM memos
WHERE id = 'your-memo-id';
```

3. **ตรวจสอบ Database Settings**:
```sql
-- ดูค่า config
SHOW app.supabase_url;
SHOW app.supabase_anon_key;
```

### Function Deploy ไม่สำเร็จ

1. ตรวจสอบว่า login และ link project แล้ว:
```bash
supabase projects list
```

2. ตรวจสอบว่าโครงสร้างไฟล์ถูกต้อง:
```
supabase/
  functions/
    telegram-notify/
      index.ts
```

3. ดู error message:
```bash
supabase functions deploy telegram-notify --debug
```

## การบำรุงรักษา

### ดูสถิติการส่งการแจ้งเตือน

```sql
-- นับจำนวนเอกสารที่เปลี่ยนสถานะใน 7 วันที่แล้ว
SELECT
  status,
  COUNT(*) as count
FROM memos
WHERE updated_at >= NOW() - INTERVAL '7 days'
GROUP BY status;
```

### ดูผู้ใช้ที่ยังไม่มี telegram_chat_id

```sql
SELECT
  user_id,
  first_name,
  last_name,
  position,
  telegram_chat_id
FROM employees
WHERE telegram_chat_id IS NULL OR telegram_chat_id = '';
```

### ปิดการแจ้งเตือนชั่วคราว

```sql
-- ปิด trigger
ALTER TABLE memos DISABLE TRIGGER telegram_notify_on_memo_status_change;
ALTER TABLE doc_receive DISABLE TRIGGER telegram_notify_on_doc_receive_status_change;

-- เปิด trigger
ALTER TABLE memos ENABLE TRIGGER telegram_notify_on_memo_status_change;
ALTER TABLE doc_receive ENABLE TRIGGER telegram_notify_on_doc_receive_status_change;
```
